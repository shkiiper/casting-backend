services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onset-backend
    restart: unless-stopped
    env_file:
      - .env.production.local
    ports:
      - "8080:8080"
